// Modules  
// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum)
const names = require("./3-firstModule");
const sayHi = require("./3-1--utils");
const data = require("./3-2--utils");
require('./3-3--mind-granade');

// sayHi("Gregory");
// sayHi(names.john);
// sayHi(names.peter);

// console.log(data);
